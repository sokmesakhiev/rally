Hi<%= @user.profile&.display_name.present? ? " #{@user.profile.display_name}" : "" %>,

We received a request to reset the password for your Rally account (<%= @user.email %>).

Reset your password: <%= @reset_url %>

This link expires in 2 hours. If you didn't request a password reset, you can safely ignore this email — your password will not be changed.

— The Rally team
